apiVersion: v1
kind: Service
metadata:
  name: fastapi-demo-service
spec:
  selector:
    app: fastapi-demo
  ports:
    - protocol: TCP
      port: 80  # Port exposed for external access
      targetPort: 8000  # The port FastAPI is listening on
